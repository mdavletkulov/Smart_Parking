<#import "../../parts/common.ftlh" as c>

<@c.page>
    <#if message?has_content>
        <div class="alert alert-${messageType} col-4" role="alert">
            ${message}
        </div>
    </#if>
    <div><h5 class="mb-3">Должности</h5>
    </div>
    <table class="table table-striped text-center table-hover col-5" id="jobs" style="width: 50%">
        <thead class="thead-dark">
        <tr>
            <th class="align-middle" scope="col">Подразделение</th>
            <th class="align-middle" scope="col">Должность</th>
            <th></th>
            <th class="align-middle" scope="col">
                <a href="/dataEdit/job/add" class="btn btn-primary stretched-link">
                    Добавить новую должность</a></th>
        </tr>
        </thead>
        <tbody>
        <#list jobs as job>
            <tr>
                <th class="align-middle" scope="row">${job.getTypeJobPosition()}</th>
                <td class="align-middle" scope="row">${job.getNamePosition()}</td>
                <td>
                    <button id="${job.getId()}" type="button" class="btn btn-primary askDelete"
                            data-toggle="modal">
                        Удалить
                    </button>
                </td>
                <td><a href="/dataEdit/job/edit/${job.id}"
                       class="btn btn-primary stretched-link">Редактировать</a>
                </td>

            </tr>
        </#list>
        </tbody>
    </table>

    <div class="modal fade" id="exampleModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel"
         aria-hidden="true">
        <div class="modal-dialog" role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="exampleModalLabel">Подтвердите удаление</h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body">
                    Вы действительно хотите удалить должность?
                </div>
                <div class="modal-footer">
                    <button id="delete" type="button" class="btn btn-secondary">Удалить</button>
                    <button type="button" class="btn btn-primary" data-dismiss="modal">Отменить</button>
                </div>
            </div>
        </div>
    </div>

    <script>
        var jobId;
        jQuery(document).ready(function ($) {

            $('#jobs').DataTable({
                "lengthMenu": [[5, 10, 25, 50, -1], [5, 10, 25, 50, "Все"]],
                columnDefs: [
                    {width: '15%', targets: 0},
                    {width: '45%',  targets: 1},
                    {width: '25%', "orderable": false, targets: 2},
                    {width: '15%', "orderable": false, targets: 3}
                ],
                language: {
                    url: '/static/localization/Russian.json'
                },
                fixedColumns: true
            });

            $(document).on('click', 'button.askDelete', function(e) {
                jQuery.noConflict();
                $('#exampleModal').modal('show');
                jobId = $(this).attr('id');
            });
        });

        $('button#delete').click(function () {
            window.location.href = '/dataEdit/job/delete/' + jobId;
            return false;
        });
    </script>

</@c.page>